Public Class SpriteFactory 'takes care of putting CharObjs together
    'properties

    'character map
    'colour map
    'background colour map
    'type of charobj map

    'charObjArray
    Public Sub New()
        'init these
        'character map
        'colour map
        'background colour map
        'type of charobj map

        'run initCharObjArray
    End Sub

    'methods
    'initCharObjArray{sets size of charobj map}

    'getRenderedSprite{returns charobjarray} 'This is essentially the object as a collection of its parts

End Class
